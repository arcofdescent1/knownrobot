# Known Robot production release — 2026-09-16

## Deployment targets

- Repository: `arcofdescent1/knownrobot`, production branch `main`.
- Vercel: `prj_nDKcvbnxaoicCwlaZlPyx9feB0I3`, team `team_rFroNfnAXmKnyUCVn3nW2NQ1`, root `embodied-registry`.
- Supabase: dedicated **Known Robot** project `lxhepwrzhpukmuoeiwgj`, `us-east-1`, in the existing **Seppi Design** Free-plan organization. No paid tier or add-on was enabled.
- With explicit user approval, **arcofdescent1's Project** (`blzyyjbwvlqmgjdbjnwh`) was paused, not deleted. PulsePlan and already-paused StoryNight were left unchanged. Resuming the old project while two free slots are in use may require first freeing a slot.

## Database release

Applied and verified matching local/remote history for:

1. `202609080001_initial_registry.sql`
2. `202609160001_verification_authority.sql`
3. `202609160002_public_evidence_registry.sql`
4. `202609160003_public_contribution_credit.sql`
5. `20260916144709_explicit_data_api_privileges.sql`

The fifth migration makes the API grants explicit under new Supabase defaults, with anonymous read-only access and no client membership writes. It does not loosen protected evaluation-column grants or reviewer authority. The local PostgreSQL harness now tests without automatic table grants.

A pre-migration custom-format backup was saved outside Git at `C:\Users\dsepp\AppData\Local\Temp\knownrobot-release-backup-820d8c71946a4539bb08e37a8855e2e8\pre-migration.dump`. This is a temporary local backup, not a recurring or off-machine backup service. The generated database password is stored in Windows Credential Manager under `Known Robot Supabase:database`, not in Git or public Vercel variables.

## Verification and security

- Remote validator, registry release and PostgreSQL authorization checks passed for the fixes/grants release.
- Hosted authorization/privacy/snapshot/retraction/pagination checks passed in a single rolled-back transaction. Confirmed zero auth users/evaluations and no fixture schema remained afterward. These tests use database role/JWT context; they are not a claim of completed real-person recruitment or end-user authentication sessions.
- The actual publishable-key Data API returns zero genuine records and zero statistics. No demonstration evaluations, reviewer appointments, teams or adapter owners were seeded.
- Vercel production has `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` (a publishable key despite the legacy variable name). No service/secret/database password was added to the website. Preview/development environments have no production database credentials.
- Security advisors reported one intentional warning: authenticated execution of the security-definer `review_evaluation` RPC. This narrow privilege boundary is necessary because clients cannot write review-controlled columns. It uses empty search_path, explicit EXECUTE grants, authenticated actor, locked active appointment, scoped status authority, independent actor checks and attributable snapshots. Anonymous and service-role review execution is revoked. The tested unauthorized paths fail closed. No other security-advisor findings were returned.
- Live checks through the production Vercel alias cover page metadata, genuine empty results and browser search. A missing-evaluation streaming soft 404 was found and a public-view-only pre-streaming existence check plus regression coverage was added.

## Outstanding domain cutover

Production code is served at `https://embodied-registry.vercel.app`. However, `knownrobot.com` currently opens a parked `/lander` page. Its authoritative nameservers are `ns05.domaincontrol.com` and `ns06.domaincontrol.com`; apex A records are `3.33.130.190` and `15.197.148.33`, and `www` aliases the parked apex. Vercel reports `misconfigured: true` despite ownership verification.

Vercel's live configuration recommends apex A values `216.150.1.1` and `216.150.16.1` and a `www` CNAME of `da65b1711c4ef1a0.vercel-dns-016.com`. Change only the conflicting parking A/www records in the authoritative registrar DNS; preserve unrelated mail/TXT records and nameservers. The live API also accepts `76.76.21.21` as a lower-ranked apex alternative. DNS propagation and HTTPS/app verification on knownrobot.com remain required. Do not redirect the working legacy alias to the parked custom domain before this is resolved.

No registrar DNS change, account-level branch protection, recurring backup service, user/reviewer appointment or real robotics evaluation was claimed as part of this release.
